package fetion

/*
import (
	"fetion/models/cache"
	"fmt"
	"io/ioutil"
	"net/http"
	"net/url"
	"testing"
	"time"
)

var (
	client *http.Client
)

func init() {
	config := &cache.RedisConfig{
		Host:              "127.0.0.1:6379",
		DefaultExpiration: 240 * time.Hour,
	}
	redisCache := cache.NewRedisCache(config)

	client = &http.Client{
		Jar: &Jar{redisCache, time.Hour * 24, "fetion"},
	}
}
*/
